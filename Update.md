# 计划更新
## goods_grid
```c++
完善单击/双击交互.
```

## 容器管理
```c++
// 容器基本框架
1.[容器管理]作为[全局节点]: 容器创建时加入管理. 
    区分[物品容器/搜索容器];

2.物品转移规则
    [默认转移]: 物品容器 <==> 搜索容器
    [拖拽转移]: 
        物品拿起时, [容器管理]持有物品数据并显示高亮.
        根据拖拽结束位置判定容器归属: 原容器不处理, 新容器则触发转移.
    双击容器中物品触发[默认转移规则].
    拖拽触发[拖拽转移]. 
```

## 奖励池
```c++
奖励池: {
    容器: {
        [
            普通: GoodsContainer,
            机密: GoodsContainer,
            绝密: GoodsContainer
        ]
    }
    //"物品": {
	//	"工艺藏品": [
	//		[
	//			{"物品名称": "绷带", "物品格数": [1, 1], "物品价值": 5}, # 物品 
	//		], #白
	//	]
	//}
    物品: {
        
    }
    
	qualitys.append({
		"物品名称": "人像照片",
		"物品类型": Goods.Class.Document,
		"物品格数": [1, 1],
		"物品价值": 1000
	})
}


```